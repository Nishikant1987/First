import random as r
import  random # This imports the random module

a = r.randint(1, 100)  # Generates a random integer between 1 and 100 and assigns it to 'a'
b = random.randint(101, 200)
print(a, b)  # Prints the value of 'a'
